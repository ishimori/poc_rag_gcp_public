# DD-012: プロジェクトドキュメント整備

| 作成日 | 更新日 | ステータス |
|--------|--------|------------|
| 2026-03-20 | 2026-03-21 | 進行中 |

## 目的

プロジェクトの設計情報を体系的にドキュメント化し、人間にも Claude にも伝わる形で整備する。
ドキュメントは一度作って終わりではなく、コード変更時に同期メンテされる仕組みを作る。

## 背景・課題

- DD-009-3 で TypeScript UI + Cloud Functions 構成に移行したが、全体像を把握できるドキュメントがない
- CLAUDE.md がテンプレートのままで、プロジェクト固有の技術スタック・規約が未記入
- 新しい会話で Claude を起動するたびに、コードベースの探索から始まる非効率
- DB定義、画面設計、環境構成が暗黙知のままになっている

## 作成するドキュメント一覧

### 1. アプリ仕様系（`doc/spec/app/`）

| ドキュメント | 配置先 | 内容 |
|-------------|--------|------|
| DB定義書 | `doc/spec/app/database.md` | Firestoreコレクション定義（chunks, query_logs 等） |
| ER図 | `doc/spec/app/database.md` 内 | mermaid erDiagram でコレクション関係を図示 |
| API仕様書 | `doc/spec/app/api.md` | エンドポイント一覧、リクエスト/レスポンス定義 |
| 画面設計書 | `doc/spec/app/screens.md` | 各画面の機能一覧・コンポーネント構成・API対応 |

### 2. 環境・デプロイ系（`doc/spec/infra/`）

| ドキュメント | 配置先 | 内容 |
|-------------|--------|------|
| 環境構成 | `doc/spec/infra/infrastructure.md` | GCPサービス構成、環境変数一覧、ローカル開発ポート |
| 開発手順 | `doc/spec/infra/development.md` | セットアップ、起動、テスト、デプロイの手順 |

### 3. Claude向け（CLAUDE.md）

| ドキュメント | 配置先 | 内容 |
|-------------|--------|------|
| プロジェクト概要 | `CLAUDE.md` | 技術スタック、ディレクトリ構成、規約、doc/spec/* へのポインタ |

## 設計方針

### Claude に伝わるドキュメントとは

1. **CLAUDE.md をハブにする** — プロジェクト概要 + 各ドキュメントへのポインタ。新しい会話の最初に読み込まれる
2. **doc/spec/ に詳細を置く** — `app/`（アプリ仕様）と `infra/`（環境・運用）に分類。必要なときだけ参照
3. **mermaid で図を書く** — テキストベースなので git diff で変更が追える。Claude も読める
4. **コードと近い場所に置く** — 乖離しにくい

### 継続メンテの仕組み

- DDの「仕様書同期」ステップ（Full フロー Step 8）で、関連ドキュメントの更新を必須にする
- CLAUDE.md に「ドキュメント更新ルール」を明記し、Claude が自発的に更新を提案できるようにする

## Phase 0 棚卸し結果

### Firestoreコレクション（2コレクション）

| コレクション | フィールド数 | 用途 |
|-------------|-------------|------|
| `chunks` | 8 | 文書チャンク + 768次元ベクトル埋め込み |
| `query_logs` | 8 | チャットクエリ・回答の記録 |

**chunks**: content, content_hash, embedding(Vector768), source_file, chunk_index, category, security_level, allowed_groups

**query_logs**: query, answer, model, elapsed_ms, sources, source_count, no_answer, timestamp

### 画面一覧（6画面）

| ルート | コンポーネント | 機能 |
|--------|-------------|------|
| `/` | App.tsx | RAGチャット（モデル選択、ソース表示） |
| `/admin` | Dashboard.tsx | 評価スコア、チャンク数、パラメータ、技術マップ |
| `/admin/tuning` | Tuning.tsx | パラメータ編集、ingest/evaluate実行 |
| `/admin/data` | DataBrowser.tsx | チャンク一覧・フィルタ・詳細表示 |
| `/admin/history` | History.tsx | 評価結果一覧・スコア推移 |
| `/admin/logs` | Logs.tsx | クエリログ一覧・フィルタ |

### APIエンドポイント（8エンドポイント）

| メソッド | パス | 機能 |
|---------|------|------|
| POST | `/api` | RAGチャット |
| POST | `/api/admin/ingest` | ドキュメントインジェスト |
| POST | `/api/admin/evaluate` | 評価実行 |
| GET | `/api/admin/evaluate/results` | 評価結果一覧 |
| GET | `/api/admin/config` | パラメータ取得 |
| PUT | `/api/admin/config` | パラメータ更新 |
| GET | `/api/admin/chunks` | チャンク一覧 |
| GET | `/api/admin/logs` | クエリログ一覧 |

### GCPサービス依存（5サービス）

| サービス | 用途 |
|---------|------|
| Firestore | ベクトルDB（chunks）+ クエリログ |
| Vertex AI | Gemini 2.5 Flash/Pro（LLM）+ text-embedding-005（埋め込み） |
| Cloud Discovery Engine | リランキングAPI |
| Cloud Functions Gen2 | chat(120s/1GB) + admin(540s/1GB)、Python 3.12 |
| Firebase Hosting | SPA配信 + API rewrite |

### 環境変数（2変数）

| 変数 | 値 | 用途 |
|------|---|------|
| `GOOGLE_CLOUD_PROJECT` | `poc-rag-490804` | GCPプロジェクトID |
| `GOOGLE_CLOUD_LOCATION` | `asia-northeast1` | Vertex AIリージョン |

### ローカル開発構成

| プロセス | ポート | 備考 |
|---------|--------|------|
| Vite (UI) | 5180 | `UI_PORT` で変更可 |
| chat API | 8081 | `API_PORT` で変更可 |
| admin API | 8082 | `ADMIN_PORT` で変更可 |

起動: `bash scripts/dev.sh` / 停止: `bash scripts/dev.sh stop`

## 決定事項

- doc/spec/ をサブフォルダで整理: `app/`（database, api, screens）+ `infra/`（infrastructure, development）
- DD-012作成時点の画面は4画面だったが、現在は6画面（DD-009-5でlogs画面が追加）。タスク一覧を現状に合わせて更新
- Phase 2のAPI仕様書は全8エンドポイントを対象にする（当初の「POST /chat + 今後追加予定」から更新）
- PH0棚卸し結果を踏まえ、1 Phase = 1ファイルの粒度にタスク一覧を再構成（PH1〜PH5）

## タスク一覧

### Phase 0: 事前精査
- [x] 📋 **現状の暗黙知の棚卸し**
  - Firestoreコレクション: 2コレクション（chunks: 8フィールド, query_logs: 8フィールド）
  - 画面: 6画面（チャット + 管理5画面）
  - API: 8エンドポイント（chat 1 + admin 7）
  - GCPサービス: 5サービス（Firestore, Vertex AI, Discovery Engine, Cloud Functions, Firebase Hosting）
  - 環境変数: 2変数（GOOGLE_CLOUD_PROJECT, GOOGLE_CLOUD_LOCATION）
- [x] 😈 **Devil's Advocate調査** — Warning 1件、Info 2件（下記DA記録参照）

### Phase 1: DB定義書（`doc/spec/app/database.md`）
- [x] Firestoreコレクション定義（`chunks` — 8フィールド）
- [x] Firestoreコレクション定義（`query_logs` — 8フィールド）
- [x] mermaid erDiagram（コレクション間の関係図）
- [x] Firestoreインデックス定義（複合2件 + ベクトル1件）
- [x] 🔬 **機械検証**: mermaid-cli で SVG 生成成功
- [x] 😈 **DA批判レビュー** — 問題なし（下記DA記録参照）

### Phase 2: API仕様書（`doc/spec/app/api.md`）
- [ ] Cloud Functions構成（chat: 120s/1GB, admin: 540s/1GB）
- [ ] 全8エンドポイントのリクエスト/レスポンス定義
- [ ] CORSポリシー・エラーレスポンス形式
- [ ] 🔬 **機械検証**: APIレスポンス定義と実際の `main.py` の出力が一致すること
- [ ] 😈 **DA批判レビュー**

### Phase 3: 画面設計書（`doc/spec/app/screens.md`）
- [ ] チャット画面（`/`）— コンポーネント構成、使用API
- [ ] 管理画面5画面（`/admin`, `/admin/tuning`, `/admin/data`, `/admin/history`, `/admin/logs`）
- [ ] 各画面のルーティング構成（React Router）
- [ ] 🔬 **機械検証**: 記載された画面・APIの対応が実コードと一致すること
- [ ] 😈 **DA批判レビュー**

### Phase 4: 環境構成・開発手順（`doc/spec/infra/infrastructure.md` + `doc/spec/infra/development.md`）
- [ ] `infrastructure.md`: GCPサービス依存一覧、Firebase構成図（mermaid）、環境変数一覧、ローカル開発ポート
- [ ] `development.md`: 前提条件、セットアップ手順、起動・停止、デプロイ手順（環境情報はinfrastructure.mdへリンク）
- [ ] 🔬 **機械検証**: development.md の手順が実際のスクリプトと一致すること
- [ ] 😈 **DA批判レビュー**

### Phase 5: CLAUDE.md 更新（プロジェクト概要 + メンテルール）
- [ ] プロジェクト固有セクション記入（技術スタック、ディレクトリ構成、コーディング規約）
- [ ] doc/spec/* へのポインタ
- [ ] ドキュメント更新ルール（DB/API/画面変更時の同期ルール）
- [ ] 🔬 **機械検証**: 新しいClaude会話でCLAUDE.mdからプロジェクト概要を正しく把握できること
- [ ] 😈 **DA批判レビュー**

### Phase 6: DD-013/DD-014 変更の doc/spec/ 反映（※他セッション完了後）
> DD-013（テスト評価基盤の品質改善）・DD-014（テストデータ充実化）が並行作業中。
> PH1〜PH4は現時点のコードで作成し、本Phaseで差分を同期する。

- [ ] DD-014 の影響反映
  - `app/database.md`: chunksコレクションのカテゴリ追加・チャンク数増加があれば反映
  - `app/screens.md`: ダッシュボードの新テストパターン表示があれば反映
- [ ] DD-013 の影響反映
  - `app/api.md`: evaluate結果のレスポンス構造変更（スコアリング改善・パターン分離等）があれば反映
  - `app/screens.md`: 評価結果表示の変更（未実装機能テストの分離表示等）があれば反映
- [ ] 🔬 **機械検証**: 更新後の doc/spec/ が実コードと一致すること

## ログ

### 2026-03-21
- Phase 0 完了: 暗黙知の棚卸し実施
  - Firestore 2コレクション、6画面、8 API、5 GCPサービス、2環境変数を確認
  - DD作成時（2026-03-20）から画面が4→6に増加（DD-009-5でlogs画面追加）
  - タスク一覧のPhase 2を現状に合わせて更新
- DA批判レビュー: Warning 1件（development.mdとinfrastructure.md境界整理）、Info 2件
- PH1〜PH5を再構成: 1 Phase = 1ファイルの粒度に変更
- doc/spec/ をサブフォルダ構成に変更: `app/`（アプリ仕様）+ `infra/`（環境・運用）
- Phase 1 完了: `doc/spec/app/database.md` 作成
  - chunks（8フィールド）、query_logs（8フィールド）のフィールド定義
  - 複合インデックス2件 + ベクトルインデックス1件
  - mermaid erDiagram（mermaid-cli で SVG 生成確認済み）
  - DA批判レビュー: Critical/Warning なし

### 2026-03-20
- DD作成

---

## DA批判レビュー記録

### Phase 0 DA批判レビュー

**DA観点:** ドキュメント構成の妥当性、形骸化リスク、粒度の適切さ

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | `development.md`（開発手順）と `infrastructure.md`（環境構成）の境界が曖昧。環境変数・ポート情報が両方に書かれると二重管理になる | Warning | 環境変数を変更 → どちらのファイルを更新すべきか迷う | 暗黙の前提 | infrastructure.md に環境構成を集約し、development.md は手順のみにする。環境変数・ポート一覧は infrastructure.md に記載し、development.md からはリンクで参照 |
| 2 | ドキュメント形骸化リスク: DDのFull フローStep 8「仕様書同期」に依存するが、Standardフロー（5ステップ）では仕様書同期ステップがない | Info | Standardフローで実装 → doc/spec/ が更新されない | 依存関係 | CLAUDE.md に「DB/API/画面変更時は doc/spec/ 同期必須」ルールを明記。フローに依存せず常時適用 |
| 3 | 5ファイル（database, api, screens, infrastructure, development）はPoCスケールでは妥当。ただし将来的にapi.mdが肥大化するリスクあり（現在8エンドポイント） | Info | — | スケーラビリティ | 現時点では1ファイルで十分。20エンドポイント超になったら分割を検討 |

### Phase 1 DA批判レビュー

**DA観点:** DB定義書の正確性・網羅性

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| — | Critical/Warning なし | — | — | — | コレクション2つ・フィールド定義はコード（store.py, main.py）と完全一致。ER図のリレーションも論理参照であることを明記済み |
