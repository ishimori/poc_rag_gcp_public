# DD-009-5: クエリログ蓄積・分析基盤

| 作成日 | 更新日 | ステータス |
|--------|--------|------------|
| 2026-03-20 | 2026-03-20 | 進行中 |

## 目的

RAGチャットの実行ログ（質問・回答・応答時間・使用モデル等）をFirestoreに蓄積し、ナレッジギャップの発見・評価データセット生成・パフォーマンス分析を可能にする。

## 背景・課題

- DD-009-3でTypeScript UIに移行し、チャット機能が動作している
- しかし「どんな質問がされたか」「どの質問に答えられなかったか」の記録がない
- PoCの価値を示すには「使われた結果」のデータが不可欠
- 蓄積されたクエリは評価データセット（DD-007）の拡充にも活用できる

## 検討内容

### 蓄積するデータ

| フィールド | 型 | 説明 |
|-----------|-----|------|
| query | string | ユーザーの質問文 |
| answer | string | LLMの回答文 |
| model | string | 使用したモデル名 |
| elapsed_ms | number | 応答時間（ミリ秒） |
| sources | array | リランク後にヒットしたソース一覧 |
| source_count | number | ヒットしたソース件数 |
| no_answer | boolean | 「記載がありません」回答だったか |
| timestamp | timestamp | 記録日時 |

### 保存先

- Firestore `query_logs` コレクション（既存のFirestoreをそのまま活用）
- Cloud Function内で回答生成後に非同期で書き込み（レスポンス遅延を防ぐ）

### 活用方法

1. **ナレッジギャップ分析** — `no_answer=true` のクエリを集めて、不足ドキュメントを特定
2. **評価データセット自動生成** — 実際の質問を `test-data/` に取り込み
3. **モデル比較** — 同じ質問に対するモデル別の回答品質・速度を比較
4. **利用傾向の可視化** — DD-009-4（管理者UI）で表示

## 決定事項

1. **ログ書き込み方式**: 同期書き込み + try-except（Cloud Functionsではレスポンス返却後にプロセスが凍結されるリスクがあるため、threadingによる非同期は不採用）
2. **no_answer判定**: 回答文に「記載がありません」を含むかで判定（flow.pyのプロンプトと対応）
3. **個人情報リスク**: PoCフェーズでは許容。本番化時にデータ保持ポリシー・TTL設定を検討

## タスク一覧

### Phase 0: 事前精査
- [x] 📋 **タスク精査・詳細化**
  - ログ書き込みの非同期処理方式の確認 → 同期+try-except方式に決定
  - `no_answer` 判定ロジックの設計 → 「記載がありません」含有チェック
- [x] 😈 **Devil's Advocate調査**
  - ログ書き込み失敗時にAPIレスポンスに影響しないか → try-exceptで問題なし
  - Firestoreの書き込みコスト（無料枠: 2万回/日）で十分か → PoCでは十分
  - 個人情報を含む質問が蓄積されるリスク → PoCでは許容、本番化時に要対策

### Phase 1: バックエンドログ書き込み
- [x] `main.py` にFirestoreログ書き込み処理を追加（同期+try-except方式）
- [x] `no_answer` 判定ロジック実装（`NO_ANSWER_MARKER = "記載がありません"` 定数化）
- [ ] 🔬 **機械検証**: curlでAPI呼び出し後、Firestoreに `query_logs` ドキュメントが作成されること
- [ ] 😈 **DA批判レビュー**

### Phase 2: 管理者向けログ閲覧（DD-009-4と連携）
- [ ] ログ一覧API（`GET /logs`）の実装
- [ ] 管理者UIにログ閲覧画面を追加
- [ ] 🔬 **機械検証**: ブラウザでログ一覧が表示されること
- [ ] 😈 **DA批判レビュー**

## ログ

### 2026-03-20
- DD作成
- PH0完了: タスク精査・DA調査
  - ログ書き込み方式 → 同期+try-except（threading非同期はCF凍結リスクで不採用）
  - no_answer判定 → 「記載がありません」含有チェック、定数 `NO_ANSWER_MARKER` で管理
  - DA調査: 個人情報リスク（中/本番化時対策）、コスト・遅延は問題なし
- PH1実装完了: main.pyにFirestoreクエリログ書き込み処理を追加
  - `_save_query_log()`: query, answer, model, elapsed_ms, sources, source_count, no_answer, timestamp
  - `time.monotonic()` による応答時間計測
  - try-exceptで書き込み失敗してもAPIレスポンスに影響しない設計
  - DA批判レビュー完了: 重大な問題なし
  - 🔬 機械検証はデプロイ後に実施予定

---

## DA批判レビュー記録

### Phase 0 DA批判レビュー

**DA観点:** ログ蓄積の設計妥当性・リスク評価

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | 個人情報がクエリログに蓄積される可能性 | 中 | 個人名を含む質問を送信 → Firestoreで確認 | セキュリティ | PoCでは許容。本番化時にTTL・匿名化を検討 |
| 2 | ログ書き込みによるレスポンス遅延 | 低 | curl計測で数十ms程度の増加 | パフォーマンス | Firestore書き込みは数十msで許容範囲 |
| 3 | no_answer判定がプロンプト文言に依存 | 低 | プロンプト変更時に判定漏れ | 保守性 | 判定文字列を定数化して対応 |

### Phase 1 DA批判レビュー

**DA観点:** 実装品質・エラーハンドリング・データ整合性

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | answer全文保存によるドキュメントサイズ増加 | 低 | 長文回答時にFirestoreドキュメントサイズ確認 | コスト | 1ドキュメント1MB上限で十分余裕あり。PoCでは許容 |
| 2 | ログ保存失敗がprintのみで通知なし | 低 | Firestore接続エラー時にログ確認 | 運用 | Cloud Functionsのログで確認可能。PoCでは十分 |
| 3 | sourcesにsource_fileのみ保存（contentは除外） | OK | — | データ設計 | ログ肥大化防止のため意図的にファイル名のみ保存 |
