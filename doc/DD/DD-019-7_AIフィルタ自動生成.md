# DD-019-7: AIフィルタ自動生成（#12 セルフクエリ）

| 作成日 | 更新日 | ステータス |
|--------|--------|------------|
| 2026-03-21 | 2026-03-21 | 進行中 |

## 目的

ユーザークエリから「最新版」「2026年版」等のフィルタ条件をAIで自動抽出し、検索時に適用することで、temporal テストタイプ（1/3 = 33%）のスコアを改善する。

## 背景・課題

DD-019-8 完了後の現状（Overall 75.7%）:
- **temporal: 1/3 (33%)** — 2件失敗（temporal-001, temporal-003）
- `leave_policy.md`（2026年版）と `leave_policy_2024.md`（2024年版）が共存
- 検索で両バージョンのチャンクが混在し、LLMが正しい回答を生成できない or 「情報なし」

### temporal テストケース（3件）

| ID | クエリ | 期待ソース | 現状 | 問題 |
|---|---|---|---|---|
| temporal-001 | 有給の繰越上限は何日？ | leave_policy.md (2026版: 20日) | FAIL | 旧版(10日)との混在 or 情報なし |
| temporal-002 | 計画年休は何日？ | leave_policy.md (2026版: 5日) | PASS | — |
| temporal-003 | 最新の休暇規程はいつ改定された？ | leave_policy.md (2025-10-01) | FAIL | 情報なし |

### 新旧バージョンの違い

| 項目 | 2024年版 (leave_policy_2024.md) | 2026年版 (leave_policy.md) |
|---|---|---|
| 最終改訂 | 2024-04-01 | 2025-10-01 |
| 繰越上限 | 10日 | **20日** |
| 計画年休 | 3日 | **5日** |

### DD-019-1 からの申し送り

> PoCでは全文書を一括Ingestするため、Ingest日が全文書で同一になり日付ボーナスが機能しない。
> DD-019-7（#12 セルフクエリ）でクエリから「2026年版」等のフィルタ条件を自動抽出する方が根本的な解法。

### 現在のFirestoreチャンクのメタデータ

| フィールド | 用途 |
|---|---|
| source_file | ファイル名（`leave_policy.md` / `leave_policy_2024.md`） |
| category | カテゴリ（`hr` 等） |
| security_level | セキュリティレベル |
| allowed_groups | アクセス許可グループ |

**`updated_at` / `doc_version` は未保存** — フィルタに使えるのは `source_file`（ファイル名に年度が含まれる）のみ。

## 検討内容

### アプローチ選択肢

| 方式 | 概要 | メリット | デメリット |
|------|------|----------|----------|
| A. LLMセルフクエリ → Firestoreフィルタ | LLMでフィルタ条件抽出 → `.where()` | 汎用的 | `updated_at` 未保存で使えるフィールドが限定的 |
| B. LLMセルフクエリ → ポスト検索フィルタ | LLMでバージョン意図抽出 → 検索後にチャンクをフィルタ | 既存スキーマで即実装可 | 検索結果が減る可能性 |
| C. LLM回答プロンプト改善 | 「最新版を優先してください」をプロンプトに追加 | 最も軽量 | 検索結果に旧版しかない場合は効果なし |

**方式B + C の組み合わせ**: セルフクエリで「最新版優先」を検出 → source_file ベースでバージョン判定 → 回答プロンプトにも最新版優先を指示。Firestoreスキーマ変更不要。

### 実装設計

**新規ファイル**: `src/search/self_query.py`
- `extract_filters(query: str)` → `QueryFilters`
  - LLMでクエリから条件抽出: `{"prefer_latest": bool, "version_hint": str | null, "category_hint": str | null}`
  - 例: 「有給の繰越上限は何日？」→ `{"prefer_latest": true}` (暗黙的に最新版を期待)
  - 例: 「2024年版の計画年休は？」→ `{"prefer_latest": false, "version_hint": "2024"}`

**変更ファイル**:
- `src/config.py` — `self_query: bool = True` 追加
- `src/search/flow.py` — search後、rerank前にバージョンフィルタを適用
- `src/search/flow.py` — `_generate_answer()` プロンプトに最新版優先ヒントを追加

**フィルタロジック** (`prefer_latest=True` の場合):
1. 検索結果の `source_file` から同名ドキュメントの新旧を判定
   - `leave_policy.md` = 最新版、`leave_policy_2024.md` = 旧版
   - 年度サフィックス（`_2024`）があるファイルは旧版とみなす
2. 同一カテゴリで新旧が混在する場合、旧版チャンクを除外（またはスコアを大幅減）
3. フォールバック: フィルタ後に結果が0件なら、フィルタなしで再実行

## 決定事項

（Phase 1 完了後に確定 — 診断結果次第で Phase 2 以降の方針を調整）

## タスク一覧

### Phase 0: 事前精査
- [x] 📋 **各Phaseのタスク精査・詳細化**
  - temporal テストケースID（temporal-001〜003）を `scripts/verify_dd_test_ids.py` で検証 → OK
  - Phase 1 に診断フェーズを追加（DA-1 指摘: 検索段階の問題が先決）
- [x] 😈 **Devil's Advocate調査**
  - **DA-1（高）過剰設計リスク**: temporal 失敗2件は「情報なし」= 検索で引けていない。バージョンフィルタ以前の問題。まず検索結果の診断が必要
  - **DA-2（中）プロンプト改善で足りる可能性**: temporal-002 が PASS → leave_policy.md のチャンクは検索に引っかかることがある。新旧混在が原因なら `_generate_answer()` プロンプト改善だけで解決する可能性
  - **DA-3（低）ファイル名ベースの限界**: `_2024` サフィックス判定は本番では通用しないが、PoC範囲なので許容

### Phase 1: temporal 失敗ケースの診断
- [ ] temporal-001（有給の繰越上限は何日？）の検索結果を取得し、上位10件の source_file と content を確認
  - leave_policy.md / leave_policy_2024.md のチャンクが含まれるか？
  - 含まれるなら → LLMが「情報なし」と判断した理由を分析（新旧混在 or チャンク内容不足）
  - 含まれないなら → 検索精度の問題（DD-019-8 と同系統）
- [ ] temporal-003（最新の休暇規程はいつ改定された？）も同様に診断
- [ ] 🔬 **機械検証**: 診断スクリプトの実行結果をログに記録
- [ ] 😈 **DA批判レビュー**

### Phase 2: 対策実施（Phase 1 結果に基づき選択）

**パターンA: 検索結果に正しいチャンクが含まれている場合**
- [ ] `src/search/flow.py` の `_generate_answer()` プロンプトに最新版優先ヒントを追加
  - before: 「コンテキストのみを使って回答」
  - after: 「同じ文書の複数バージョンがある場合は、最新版（年度が新しい方）を優先して回答」
- [ ] 🔬 **機械検証**: temporal 3件を個別実行して回答内容を確認
- [ ] 😈 **DA批判レビュー**

**パターンB: 検索結果に正しいチャンクが含まれていない場合**
- [ ] 原因を特定（DD-019-8 と同系統のベクトル検索問題か？キーワード検索の漏れか？）
- [ ] 原因に応じた対策を実施
- [ ] 🔬 **機械検証**: 対策後に temporal 3件を個別実行して確認
- [ ] 😈 **DA批判レビュー**

**パターンC: A+Bの組み合わせが必要な場合**
- [ ] プロンプト改善 + 検索改善の両方を実施
- [ ] 必要ならセルフクエリモジュール（`src/search/self_query.py`）を追加
- [ ] 🔬 **機械検証**: 対策後に temporal 3件を個別実行して確認
- [ ] 😈 **DA批判レビュー**

### Phase 3: 評価
- [ ] 🔬 **機械検証**: `python scripts/evaluate.py` フル評価（74件）、temporal スコアの変動を確認
- [ ] 😈 **DA批判レビュー**

### Phase 4: 記録
- [ ] DD-019-7 ログに結果を記録
- [ ] DD-019 の子DD一覧のステータスを更新

## ログ

### 2026-03-21
- DD作成
- DD-019-6（#13 ルーティング）をスキップし、本DDを #13 なしで独立実装する方針
- DD-019-8 完了後のスコア（Overall 75.7%, temporal 1/3=33%）を踏まえて計画策定
- **Phase 0 完了**
  - テストケースID検証 OK
  - DA調査で「検索段階の問題が先決」を発見 → Phase 1 に診断フェーズを追加
  - Phase 2 を分岐構造（パターンA/B/C）に変更し、診断結果に基づき対策を選択する方針

---

## DA批判レビュー記録

<!-- DA批判レビューの手順・品質フィルター・再チェック条件は doc/da-method.md を参照 -->

### Phase 0 DA批判レビュー

**DA観点:** セルフクエリモジュールの実装前に、本当にバージョンフィルタが必要かを検証すべき

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | temporal 失敗2件は「情報なし」回答 = 検索で引けていない可能性。バージョンフィルタ以前の問題 | 高 | temporal-001, 003 の検索結果を取得 → leave_policy チャンクが含まれるか確認 | 前提検証 | Phase 1 に診断フェーズ追加 |
| 2 | temporal-002 が PASS → プロンプト改善だけで解決する可能性 | 中 | temporal-002 の検索結果と回答を確認 → 新旧チャンクの混在状況を把握 | 最小手段 | Phase 2 をパターン分岐に変更 |
| 3 | ファイル名 `_2024` サフィックスでの旧版判定は本番で通用しない | 低 | — | 汎用性 | PoC範囲なので許容。本番ではメタデータ設計が別途必要 |
