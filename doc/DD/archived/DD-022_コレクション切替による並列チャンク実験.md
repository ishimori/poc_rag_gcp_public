# DD-022: コレクション切替による並列チャンク実験

| 作成日 | 更新日 | ステータス |
|--------|--------|------------|
| 2026-03-22 | 2026-03-22 | 完了 |

## 目的

1. チャンクサイズの異なる複数コレクションを Firestore 上に並存させ、CLI から並列 Ingest できるようにする
2. Tuning 画面を「Operations Monitor」に改修し、複数ジョブの進捗監視 + コレクション切替を実現する

## 背景・課題

### 現状の問題

- チャンクサイズ実験（DD-019-9）では、1つの `chunks` コレクションを使い回して順次実行するため、**実験が直列でしか回せない**
- 実験後にチャットで回答品質を体感確認したくても、再 Ingest しないと別パラメータに切り替えられない
- スコアだけでは判断が難しいケース（「スコアは低いが実用上は十分」等）を検証する手段がない

### Tuning画面の現状と課題

現在のTuning画面（`ui/src/admin/Tuning.tsx`）は以下の機能を持つ:
- パラメータ編集（chunk_size, top_k 等）
- Technique Toggles（ヘッダーインジェクション等）
- Ingest / Evaluate / Re-tune ボタン
- 進捗バー（1ジョブのみ）

**問題点:**
- パラメータ編集・Technique Toggles → `config.py` やCLI引数で十分。UIで変えると「今どっちが正？」になる
- Re-tune → CLIスクリプトの方が並列対応しやすい
- 進捗バー → 1ジョブ前提で、複数コレクションの並列Ingestに対応できない
- タスクステータスが `task_id="ingest"` の1件固定（`src/task_status.py`）

### DD-019-9 実験結果（確定）

| パラメータ | chunks | Overall | semantic | noise_resistance | cross_category |
|-----------|--------|---------|----------|-----------------|----------------|
| baseline (800/150) | 403 | **79.7%** | 58% | 50% | 100% |
| A (600/100) | 593 | **82.4%** | 58% | 50% | 80% |
| B (1200/200) | 292 | **85.1%** | 67% | 67% | 100% |

**傾向:** チャンクが大きいほどスコアが高い。1200が最良だが、さらに大きくすると改善するか、頭打ちかは不明。

## 検討内容

### チャンクサイズの提案

DD-019-9 の結果から、**大きいチャンクが有利**という傾向が明確。以下の3コレクションを提案:

| コレクション | chunk_size | chunk_overlap | 狙い |
|-------------|-----------|--------------|------|
| `chunks_1200` | 1200 | 200 | DD-019-9 の最良結果を保持 |
| `chunks_1600` | 1600 | 300 | さらに大きくして改善幅を探る |
| `chunks_2000` | 2000 | 400 | 上限探索（頭打ちポイントの特定） |

**理由:**
- 600→800→1200 と大きくするほどスコアが上がっている（79.7→82.4は逆転だが、semantic以外はチャンク大が有利）
- contextual_retrieval=ON なので、大チャンクでも検索精度が落ちにくい
- 1200 は確定データがあるので比較基準になる
- 2000 を試すことで「どこで頭打ちか」が分かる

### 影響範囲

`config.collection_name` を参照している箇所:

| ファイル | 用途 |
|---------|------|
| `src/search/retriever.py:39` | ベクトル検索 |
| `src/search/keyword_searcher.py:29` | キーワード検索 |
| `src/ingest/store.py:30,76` | チャンク保存・クリア |
| `src/browse/exporter.py:30` | データブラウザ |
| `main.py:585` | チャット検索 |
| `scripts/ingest.py:25` | Ingest スクリプト |

すべて `config.collection_name` を間接参照しているため、**Config の値を変えるだけで全コードが追従する**。

### 設計案

#### 1. コレクション命名規則

```
chunks              ← 本番（デフォルト）
chunks_1200         ← chunk_size=1200
chunks_1600         ← chunk_size=1600
chunks_2000         ← chunk_size=2000
```

#### 2. Config 変更

`src/config.py` の `collection_name` を環境変数対応にする:
```python
collection_name: str = os.environ.get("COLLECTION_NAME", "chunks")
```

#### 3. タスクステータスの複数対応

`src/task_status.py` の `task_id` をコレクション別にする:
- 現状: `task_id = "ingest"` （1件固定）
- 変更後: `task_id = "ingest:chunks_1200"` （コレクション別）

Evaluate も同様: `task_id = "evaluate:chunks_1200"`

**命名規則:** `{操作}:{コレクション名}` — コロン区切りで操作とコレクションを分離。一覧取得時にプレフィクスフィルタしやすい。

**新規関数: `list_tasks(prefix)`**

```python
def list_tasks(prefix: str = "") -> list[dict]:
    """task_status コレクションからプレフィクスに一致するドキュメントを一覧取得する。

    Args:
        prefix: タスクIDのプレフィクス（例: "ingest:" → ingest系全部）
                空文字なら全件。
    Returns:
        [{"task_id": "ingest:chunks_1200", "running": True, "current": 3, ...}, ...]
    """
```

実装: Firestoreは前方一致クエリが可能（`>=` prefix, `<` prefix + '\uffff'`）。
`task_status` コレクションのドキュメント数は高々 10 件程度なので、全件取得してフィルタでも問題ない。
シンプルに全件取得 + Pythonフィルタを採用する。

#### 4. API エンドポイント設計

→ 詳細: [DD-022/api_design.md](DD-022/api_design.md)

新規3エンドポイント:
- `GET /collections` — コレクション一覧（チャンク数・評価スコア付き）+ アクティブコレクション名
- `PUT /collections/active` — アクティブコレクション切替（`config.collection_name` 書換）
- `GET /tasks` — タスク一覧（`?prefix=ingest:` でフィルタ可能）

既存6エンドポイントの task_id を `f"{op}:{config.collection_name}"` パターンに変更（後方互換維持）。

#### 5. データフロー図・タスク状態遷移図

→ 詳細: [DD-022/data_flow.md](DD-022/data_flow.md)

CLI（`run_chunk_experiments.py`）→ Firestore（`task_status/`, `chunks_*/`, `eval_results/`）→ UI（Operations Monitor）のデータフロー。
タスクは `running: true` → 処理中（ポーリング）→ 正常完了 or キャンセル → `running: false` の遷移。

#### 6. Tuning画面 → Operations Monitor に改修

→ 詳細: [DD-022/ui_design.md](DD-022/ui_design.md)
→ HTMLモック: [DD-022/mock_operations_monitor.html](DD-022/mock_operations_monitor.html)

廃止: パラメータ編集、Technique Toggles、Re-tune → CLI に一本化。
新設: コレクション選択ドロップダウン、複数ジョブ進捗一覧、スコア比較テーブル。

> **方針変更（2026-03-22）:** コレクション選択はチャット画面ではなくOperations Monitorに配置。

#### 7. 実験スクリプトの改修

`scripts/run_chunk_experiments.py` を改修:
- 各実験で `config.collection_name` を `chunks_{chunk_size}` に設定
- `task_id` を `f"ingest:chunks_{chunk_size}"` に設定（UIから進捗監視可能にする）
- 既存コレクション `chunks` は触らない

`scripts/ingest.py` に CLI 引数を追加:
```
python scripts/ingest.py --chunk-size 1200 --chunk-overlap 200 --collection chunks_1200 --clear
```

引数なしの場合は既存動作（`config.py` のデフォルト値）を維持。

## 決定事項

- チャンクサイズは 1200 / 1600 / 2000 の3パターンで実験（DD-019-9 の結果に基づく）
- Tuning画面はOperations Monitorに改修（パラメータ編集・Re-tuneは廃止、CLIに一本化）
- コレクション選択はOperations Monitorに配置（チャット画面には置かない）
- 実行トリガーはCLI、進捗モニタリングとコレクション切替は管理画面

## タスク一覧

### Phase 0: 事前精査
- [x] 📋 DD-019-9 の全実験結果を確認し、試すチャンクサイズを確定 → 1200/1600/2000
- [x] 📋 各 Phase のタスク詳細化
- [x] チャンクサイズは DD-019-14 の結果を待たずに着手可能と判断（チャンクサイズとプリセットは独立した関心事）
- [x] 😈 **Devil's Advocate調査**
  - コレクション増加の Firestore コスト → **問題なし**。現状 ~400 docs / 50KB。3コレクション増でも月額 $0.01 未満。コスト支配要因は LLM 推論（80%）
  - 削除フロー → **既存の `clear_collection()` で対応可能**。`config.collection_name` を切り替えて呼ぶだけ。TTL 不要
  - chunk_size=2000 の embedding 品質 → **問題ない見込み**。text-embedding-005 の入力上限は 2,048 トークン。日本語 2000 文字 ≒ 1,000〜1,500 トークンで上限内。ただしコードに truncation 処理がないため、上限超え時の挙動は Phase 1 で確認する

### Phase 1: Config & バックエンド（コレクション切替基盤 + API）
- [x] `src/config.py`: `collection_name` を環境変数 `COLLECTION_NAME` 対応に変更（before: `"chunks"` 固定 → after: `os.environ.get("COLLECTION_NAME", "chunks")`）
- [x] `src/task_status.py`: `list_tasks(prefix)` 関数を追加（設計案 §3。全件取得 + Pythonフィルタ）
- [x] `scripts/ingest.py`: CLI 引数 `--chunk-size`, `--chunk-overlap`, `--collection` を追加。argparse使用。引数指定時は `config.chunk_size` / `config.collection_name` を上書き
- [x] `scripts/ingest.py`: task_id を `"ingest"` 固定 → `f"ingest:{config.collection_name}"` に変更
- [x] `scripts/run_chunk_experiments.py`: 実験ごとに `config.collection_name = f"chunks_{chunk_size}"` を設定、`task_id` も `f"ingest:chunks_{chunk_size}"` に
- [x] `main.py` ルーティング追加: `GET /collections`, `PUT /collections/active`, `GET /tasks`
- [x] `main.py` `_handle_collections()`: `db.collections()` で `chunks*` プレフィクスのコレクションを列挙、ドキュメント数を取得
- [x] `main.py` `_handle_collections_switch()`: `config.collection_name` を書き換え
- [x] `main.py` `_handle_tasks()`: `list_tasks(prefix)` を呼び出し。`?prefix=` クエリパラメータ対応
- [x] `main.py` 既存 API 変更: `_handle_ingest_status` / `_handle_ingest_cancel` / `_handle_ingest` / `_handle_evaluate*` の task_id を `f"{op}:{config.collection_name}"` パターンに変更
- [x] `main.py` `_save_query_log()`: `"collection": config.collection_name` フィールドを追加
- [x] 🔬 **機械検証**: `python -c "from src.config import config; print(config.collection_name)"` → `chunks` ✅
- [x] 🔬 **機械検証**: `COLLECTION_NAME=chunks_test python -c "from src.config import config; print(config.collection_name)"` → `chunks_test` ✅
- [x] 🔬 **機械検証**: `python scripts/ingest.py --help` で `--chunk-size`, `--chunk-overlap`, `--collection` が表示されることを確認 ✅
- [x] 🔬 **結合テスト**: `chunks_test` コレクションに1ファイルをIngest → task_status書込・読取・クリア + Embedding + Firestore保存 全て成功 ✅
- [x] 😈 **DA批判レビュー** — DA-1（高）: 既存UIのtask_id互換性破壊 → Phase 2で対応

### Phase 2: Tuning UI → Operations Monitor 改修
- [x] `ui/src/admin/api.ts`: 新規API関数を追加 — `getCollections()`, `setActiveCollection(name)`, `getTasks(prefix?)`。型定義追加
- [x] `ui/src/admin/Tuning.tsx`: パラメータ編集・Technique Toggles・Re-tune を全削除、Operations Monitor に全面書き換え
- [x] `ui/src/admin/Tuning.tsx`: コレクション選択ドロップダウン追加（`GET /collections` → ドロップダウン、選択時に `PUT /collections/active`）
- [x] `ui/src/admin/Tuning.tsx`: 進捗表示を複数ジョブ対応に改修（`GET /tasks` を2秒ポーリング、コレクション別プログレスバー + キャンセルボタン）
- [x] `ui/src/admin/Tuning.tsx`: コレクション一覧テーブル追加（チャンク数・active状態表示）
- [x] `ui/src/admin/Logs.tsx`: `QueryLog` 型に `collection` を追加、テーブル + Detailパネルにコレクション名表示
- [x] `main.py` `_handle_logs()`: `collection` と `techniques` フィールドをレスポンスに追加
- [x] `ui/src/admin/admin.css`: Operations Monitor 用スタイル追加
- [x] 🔬 **機械検証**: 👀 Playwright目視確認 — 複数コレクション表示・切替動作OK（スクリーンショット: `doc/DD/DD-022/screenshot_multi_collections.png`）
- [ ] 😈 **DA批判レビュー**

### Phase 3: 実験実行 & 体感比較
- [x] 7コレクション（200/400/600/800/1200/1600/2000）の Ingest を CLI で並列実行（200は中止）
- [x] Operations Monitor で進捗を確認 — 複数ジョブの並列表示が正常動作
- [ ] コレクションを切り替えながらチャットで同じ質問を投げて比較
- [x] 🔬 **機械検証**: フル評価を各コレクションで実行し、スコアを比較 → 600 が最適（77.0%）
- [ ] 😈 **DA批判レビュー**

### Phase 4: 記録
- [x] DD-022 ログに結果を記録
- [x] 最適パラメータ判断: chunk_size=600 が最適だが、本命は Vertex AI Search に移行予定のため `config.py` デフォルト変更は見送り
- [x] `doc/record/rag_improvement_history.md` にチャンクサイズ実験の結果を追記
- [x] `doc/spec/app/database.md` を更新（複数コレクション構成・task_status・query_logs拡張・セキュリティフィルタ・ER図更新）

## ログ

### 2026-03-22
- DD作成
- DD-019-9 の実験結果確定: A(600)=82.4%, B(1200)=85.1%, baseline(800)=79.7%
  - 大きいチャンクほどスコアが高い傾向。1200/1600/2000 の3パターンで実験する方針に決定
- Tuning画面の方針を議論:
  - パラメータ編集・Re-tune はCLIに一本化（UIは「CLIのGUIラッパー」にすぎず存在意義が薄い）
  - 進捗モニタリングはUIの価値がある（リアルタイム進捗、キャンセル）
  - → Operations Monitor として複数ジョブの進捗一覧 + コレクション切替に特化
- HTMLモック作成: `doc/DD/DD-022/mock_operations_monitor.html`
- コレクション選択をOperations MonitorからチャットUIのサイドバーに移動する方針に変更
  - 使う場所（チャット）と操作する場所を一致させる方が直感的
  - Operations Monitorは純粋にモニター専用にする
- チャット画面モック追加: `doc/DD/DD-022/mock_chat_collection_selector.html`
- Operations Monitorモックからコレクション選択セクションを削除
- **方針変更**: コレクション選択をチャット画面→Operations Monitorに戻す。チャット画面はシンプルに保つ方針（DD-021破棄と同じ判断）。設定変更は管理画面に集約
- Phase 2.5（チャットUI）を廃止し、コレクション選択をPhase 2（Operations Monitor）に統合
- API設計詳細化: `GET /collections`, `PUT /collections/active`, `GET /tasks` のエンドポイント仕様を確定
- task_id命名規則を確定: `{操作}:{コレクション名}` — 例: `ingest:chunks_1200`
- `task_status.py` に `list_tasks(prefix)` を追加する設計を確定（全件取得+Pythonフィルタ）
- Phase 1/Phase 2 のAPI系タスク重複を解消（APIはPhase 1、UIはPhase 2に集約）
- データフロー図・タスク状態遷移図・UI→APIマッピング表を追加

- **Phase 1 実装完了**（コミット `61f05ca`）:
  - `config.py`: collection_name 環境変数対応
  - `ingest.py`: CLI引数 + task_idコレクション別
  - `task_status.py`: `list_tasks(prefix)` 追加
  - `run_chunk_experiments.py`: コレクション別対応
  - `main.py`: `GET /collections`, `PUT /collections/active`, `GET /tasks` API追加
- **Phase 1 結合テスト成功**: `chunks_test` コレクションに1ファイルIngest → task_status + Embedding + Firestore全て動作確認。テスト後クリーンアップ済み
- Phase 1 残タスク完了: 既存API task_id変更、query_log collectionフィールド追加、DA批判レビュー
- DA-1（高）: 既存Tuning UIがtask_id変更で壊れる → Phase 2 でUI改修時に対応（それまではCLIで代替）
- **Phase 2 実装完了**:
  - Tuning画面を Operations Monitor に全面書き換え（パラメータ編集・Toggles・Re-tune 全削除）
  - コレクション選択ドロップダウン + コレクション一覧テーブル + 複数ジョブ進捗表示
  - Logs にcollection/techniques カラム追加
  - Playwright で目視確認: 複数コレクション表示・切替動作OK
  - DA-1（既存UIの後方互換性破壊）はPhase 2で解消（UIを全面書き換えしたため）
- **Phase 3 実施**:
  - 4コレクション（800/1200/1600/2000）で並列 Ingest + Evaluate を実施
  - Firestore インデックス作成が必要（初回のみ。コレクションごとに2種類）
  - **Shadow Retrieval 誤発動バグ発見・修正**: `flow.py` で hybrid 結果と vector 結果を比較していたため、検索方式の違いでほぼ全クエリで誤検知。hybrid 同士で比較するよう修正
  - `--limit 5` で部分評価して修正確認後、フル評価を再実行
  - **結果（バグ修正後の公平な比較）**:

    | コレクション | Overall | 特筆 |
    |-------------|---------|------|
    | **chunks_800** | **74.3%** | security 100%, table_extract 100% |
    | chunks_1200 | 73.0% | -1.3pt |
    | chunks_1600 | 71.6% | -2.7pt |
    | chunks_2000 | 66.2% | unanswerable 20%, temporal 33% |

  - **DD-019-9 の結論（大きいほど良い）が覆った** — バグ修正後は小さいチャンクの方が高精度
  - 大チャンクの弱点: unanswerable判定が甘くなる、表データがノイズに埋もれる、手順が冗長化
  - → さらに小さいチャンク（200/400/600）の追加実験を実施
  - **追加実験結果（全6パターン最終比較）**:

    | チャンクサイズ | chunks | Overall | semantic | security | unanswerable | table_extract |
    |-------------|--------|---------|----------|----------|-------------|--------------|
    | **600** | 276 | **77.0%** | 58% | 20% | 100% | 60% |
    | **400** | 289 | **77.0%** | 58% | 40% | 100% | 60% |
    | 800 | 403 | 74.3% | 42% | 100% | 60% | 100% |
    | 1200 | 292 | 73.0% | 42% | 80% | 60% | 80% |
    | 1600 | 231 | 71.6% | 42% | 80% | 40% | 60% |
    | 2000 | 185 | 66.2% | 42% | 80% | 20% | 60% |
    | 200 | — | 中止 | — | — | — | — |

  - **結論**: スイートスポットは **chunk_size=600** 付近
    - 小チャンク（400-600）: semantic +16pt, unanswerable +40pt vs 800。検索精度が高い
    - 小チャンクの弱点: security が低い（600=20%, 800=100%）。チャンクが小さいと権限メタデータが分断される可能性
    - 400 と 600 はほぼ同等。400 より小さくする意味はない（200 は遅すぎて中止）
    - **security の問題を解決すれば 80%+ が見込める**

### DD-019-14 からの申し送り

- `scripts/ingest.py` に `--chunk-size` / `--chunk-overlap` 引数がないため、chunk_size を変えて Ingest するには config を Python コード内で上書きするワークアラウンドが必要だった
- `exec()` では `__file__` が未定義で `ingest.py` を直接実行できない問題もあった
- DD-022 でコレクション切替を実装する際に、`ingest.py` に CLI 引数（`--chunk-size`, `--collection` 等）を追加して解決すべき
- また、task_status を経由しないルートで Ingest すると UI に進捗が表示されない点も、CLI 設計時に考慮が必要

---

## DA批判レビュー記録

<!-- DA批判レビューの手順・品質フィルター・再チェック条件は doc/da-method.md を参照 -->

### Phase 1 DA批判レビュー

**DA観点:** task_id 命名規則変更による後方互換性の破壊

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | 既存Tuning UIが壊れる — `GET /ingest/status` が `task_id="ingest"` を期待するが `f"ingest:{collection}"` に変わった | 高 | Tuning画面 → Ingest実行 → 進捗バーが動かない | 後方互換 | Phase 2 でUI改修時に対応。それまではCLIで代替 |
| 2 | `_handle_collections` のドキュメント数取得が全件stream — 大規模コレクションでタイムアウトリスク | 低 | 10,000チャンク超のコレクション（現状最大600件） | パフォーマンス | 現状規模では問題なし |
| 3 | `PUT /collections/active` でコレクション存在バリデーションなし | 中 | `{"name": "nonexistent"}` → config変更 → 検索エラー | 入力検証 | Phase 2 でUI側ドロップダウンにより防止 |
