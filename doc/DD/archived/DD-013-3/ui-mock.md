# DD-013-3: 画面モック

## 現状（Before）

### Evaluateカード — アイドル時

```
┌─────────────────────────────────────────────────┐
│  [精度評価（Evaluate）]                            │
│  テストケースで質問→回答し、正答率を測定する           │
└─────────────────────────────────────────────────┘
```

### Evaluateカード — 実行中

```
┌─────────────────────────────────────────────────┐
│  [評価中...]  ← disabled、グレーアウト              │
│  テストケースで質問→回答し、正答率を測定する           │
└─────────────────────────────────────────────────┘
```

**問題**: 10分以上このまま。進捗が見えない。

---

## 改善後（After）

### Evaluateカード — アイドル時（変更なし）

```
┌─────────────────────────────────────────────────┐
│  [精度評価（Evaluate）]                            │
│  テストケースで質問→回答し、正答率を測定する           │
└─────────────────────────────────────────────────┘
```

### Evaluateカード — 実行中

```
┌─────────────────────────────────────────────────┐
│  [評価中...]  ← disabled                          │
│  テストケースで質問→回答し、正答率を測定する           │
│                                                   │
│  ┌─────────────────────────────────────────────┐ │
│  │  12 / N 件完了                               │ │
│  │  [============>                     ] 18.8%  │ │
│  │  2:15 経過 ・ 残り約 10:15                     │ │
│  │                                               │ │
│  │  最新: exact-005 → PASS (correct)             │ │
│  └─────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

### Evaluateカード — 完了（変更なし、既存のEvaluation Resultセクションが表示される）

```
┌─────────────────────────────────────────────────┐
│  [精度評価（Evaluate）]                            │
│  テストケースで質問→回答し、正答率を測定する           │
└─────────────────────────────────────────────────┘

  ↓ 別セクション（既存）

┌─────────────────────────────────────────────────┐
│  Evaluation Result                               │
│  32.8%  (21/64 passed)                           │
│  ...                                             │
└─────────────────────────────────────────────────┘
```

---

## Re-tune時のEvaluateフェーズ

Re-tune（一括実行）の中でもEvaluateフェーズに入ったら同じ進捗表示が出る。

```
┌─────────────────────────────────────────────────┐
│  [Re-tune中...]  ← disabled                      │
│  上の3ステップをまとめて実行する                      │
└─────────────────────────────────────────────────┘

  ↓ Evaluateフェーズに入ると

┌─────────────────────────────────────────────────┐
│  [精度評価（Evaluate）]  ← disabled                │
│  テストケースで質問→回答し、正答率を測定する           │
│                                                   │
│  ┌─────────────────────────────────────────────┐ │
│  │  12 / N 件完了                               │ │
│  │  [============>                     ] 18.8%  │ │
│  │  2:15 経過 ・ 残り約 10:15                     │ │
│  │                                               │ │
│  │  最新: exact-005 → PASS (correct)             │ │
│  └─────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

---

## CLI進捗表示

### Before

```
=== RAG Evaluation ===

Loaded 74 evaluation cases.

  [exact-001] 社内VPNの設定方法を教えて
    → PASS (llm: correct, keyword: 100%)
  [exact-002] パスワードリセット手順は？
    → FAIL (llm: incorrect, keyword: 0%)
  ...
```

### After

```
=== RAG Evaluation ===

Loaded N evaluation cases.

  [1/N] PASS exact-001 (llm: correct, keyword: 100%) [0:08 elapsed]
  [2/N] FAIL exact-002 (llm: incorrect, keyword: 0%) [0:18 elapsed, ~10:12 remaining]
  [3/N] SKIP ambiguous-001 (requires: clarification) [0:18 elapsed, ~10:02 remaining]
  ...
  [N/N] PASS noise-005 (llm: correct, keyword: 80%) [9:45 elapsed]

  Skipped: 10 cases (requires unimplemented features)
```

### After（--limit指定時）

```
=== RAG Evaluation ===

Loaded N evaluation cases (limit: 5).

  [1/5] PASS exact-001 (llm: correct, keyword: 100%) [0:08 elapsed]
  [2/5] FAIL exact-002 (llm: incorrect, keyword: 0%) [0:18 elapsed, ~1:12 remaining]
  ...
  [5/5] PASS exact-005 (llm: correct, keyword: 60%) [1:05 elapsed]

  ⚠ Limited run (5/N cases). Results are partial.
```
