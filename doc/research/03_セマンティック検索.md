第3回、開始します！
データが綺麗に切り分けられ（第1回）、適切にタグ付け（第2回）されたら、次はそれらを「どう探し出すか」という**検索エンジン（Retrieval）の設計**が精度の鍵を握ります。

3000人規模の会社では、「PCが重い」といった曖昧な悩みと、「ネジ番号999999」という極めて厳密な照会が混在します。これらを両立させるための**「ハイブリッド検索」**と**「クエリ拡張」**の技術を、Google Cloudのスタックで深掘りします。

---

## 第3回：セマンティック検索とハイブリッド検索の設計

RAGにおける「検索の失敗」は2種類あります。
1. **見落とし（Low Recall）**: 関連する資料があるのに見つけられない。
2. **ノイズ（Low Precision）**: 関係ない資料を拾ってしまい、AIが混乱する。

これらを防ぐためのエンジニアリング手法を解説します。

### 1. ベクトル検索（セマンティック検索）の限界を知る
ベクトル検索は「意味の近さ」で探すため、「電源が入らない」に対して「起動しない」という資料を見つけるのは得意です。しかし、**「型番・専門用語・短い略称」には極めて弱い**という特性があります。

* **例**: 「ネジ番号 999999」をベクトル化すると、AIは「999998」や「ネジの画像」を「近い」と判断してしまい、肝心の「999999」を外すことがあります。

### 2. Google Cloud での「ハイブリッド検索」の実装
この弱点を補うのが、**「キーワード（完全一致）検索」と「ベクトル（意味）検索」の併用**です。

* **Firestoreでのフィルタリング**:
    第2回で付与したメタデータを利用します。
    ```typescript
    // Genkit/Firestoreでの検索イメージ
    const results = await firestore.collection('chunks')
      .where('parts_no', '==', '999999') // 厳密なキーワードマッチ
      .findNearest('embedding', queryVector, {
        limit: 5,
        distanceMeasure: 'COSINE'
      });
    ```
* **エンジニアの工夫**: 
    ユーザーの質問から「型番らしき文字列」を正規表現やLLMで抽出。型番がある場合は「キーワードフィルタ」を優先し、ない場合は広範な「ベクトル検索」を行う**ルーティング・ロジック**を Cloud Functions 内に実装します。



### 3. クエリ拡張（Query Expansion）による網羅性の向上
ユーザーの質問文をそのまま検索に使うのは、実は非効率です。質問文が短すぎたり、言葉が足りなかったりするからです。

* **Multi-Query手法**:
    LLM（Gemini）を使い、1つの質問から「検索用のバリエーション」を3〜5つ生成させます。
    * **質問**: 「VPNが繋がらない」
    * **拡張**: 「VPN 接続エラー」「テレワーク ログイン 失敗」「AnyConnect 認証エラー」
* **メリット**: 
    1つのクエリではヒットしなかった隠れた正解を、別のキーワードが釣り上げてくれる確率が大幅に上がります。



### 4. 検索結果の「スコア」の扱い
FirestoreなどのベクトルDBは、検索結果と一緒に「距離（スコア）」を返します。

* **閾値（Threshold）の設計**:
    「似ている順に5件」を常に取ってくるのではなく、「スコアが0.7以下のものは、たとえ上位でもゴミ（無関係）とみなして捨てる」というロジックを挟みます。
* **なぜ重要か？**: 
    全く関係ない資料をLLMに渡すと、LLMは「渡された以上、ここから答えを作らなきゃ」と無理をして**ハルシネーション（嘘）**を誘発するからです。

---

## エンジニアへのアドバイス：第3回のチェックポイント

第1弾で精度が出なかった原因の一つに、**「検索のヒット率（Recall）」の低さ**が挙げられます。

1.  **「ネジ番号」のような固有名詞を、ベクトル検索だけで探そうとしていませんか？**（メタデータフィルタを使いましょう）
2.  **ユーザーの入力した「生の声」だけで検索していませんか？**（LLMで検索クエリを整形・拡張しましょう）
3.  **スコアの低い「ハズレ資料」をLLMに渡してしまっていませんか？**

---

### 次回：第4回「リランキング（再順位付け）とコンテキスト最適化」
検索で「候補」を30件持ってきたとしても、LLMに30件全部渡すのはコストと精度の面で非効率です。
* **Vertex AI Ranking API** を使い、30件を「真に正しい順」に並べ替える技術。
* LLMが最も読みやすい順番に情報を整理する「コンテキスト配置」のコツ。

ここまでの「ハイブリッド検索」や「クエリ拡張」について、実装イメージで気になる点はありますか？あるいは、「こういう変わった質問が来ることがあるんだけど、検索できる？」といった具体的なケースがあれば教えてください。