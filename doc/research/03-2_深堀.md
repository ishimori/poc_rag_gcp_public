失礼いたしました。3,000人規模のエンタープライズ実装に耐えうる「エンジニアが唸るレベル」の深掘りが不足していましたね。

第3回「検索エンジン（Retrieval）の設計」について、Google Cloudの内部仕様や数学的背景、実装上のクリティカルなトレースポイントを含めて、情報の解像度を一段跳ね上げて解説します。

---

## 第3回：検索エンジンの高度な設計 — ハイブリッド・アーキテクチャの極致

RAGの精度が「出ない」最大の技術的要因は、**「情報の分布（Vector Space）とキーワードの厳密性（Boolean Space）の乖離」**にあります。これをどうコードとインフラで埋めるか、詳細に踏み込みます。

### 1. 埋め込みモデル（Embedding）の戦略的選択
単に `text-embedding` を使うのではなく、次元数とタスクタイプを最適化します。

* **Model**: `text-multilingual-embedding-002` (Google Cloud)
* **次元数の最適化 (Dimensionality)**:
    デフォルトの768次元ではなく、特定のユースケース（例：ネジ番号の特定など）では、情報を凝縮するために次元数を調整、あるいは出力ベクトルを正規化して **Cosine Similarity** での計算効率を最大化します。
* **Task Type パラメータ**:
    API呼び出し時に `task_type="RETRIEVAL_QUERY"`（質問時）と `"RETRIEVAL_DOCUMENT"`（インジェスト時）を明示的に使い分けることで、クエリとドキュメント間のベクトル空間におけるアライメント（整列）を最適化します。

### 2. ハイブリッド検索の「スコアリング」アルゴリズム
「ベクトル検索の結果」と「キーワード検索の結果」は、そのままでは足し算できません。値のレンジ（分布）が異なるからです。

* **RRF (Reciprocal Rank Fusion)**:
    異なる検索ロジックの「順位」のみを用いてスコアを統合するアルゴリズムを採用します。
    $$\text{RRFScore}(d) = \sum_{r \in R} \frac{1}{k + r(d)}$$
    （ここで $k$ は定数、通常は 60。$r(d)$ はドキュメント $d$ の各検索手法での順位）
    これにより、「意味的に近い」ものと「型番が完全一致したもの」のバランスを数学的に安定させます。
* **Google Cloudでの実装**:
    Firestore Vector Search はベクトルの「距離」を返しますが、全文検索（キーワード）には **Cloud Search** または **Elasticsearch/OpenSearch** をサイドカーとして動かし、Cloud Functions 上で RRF ロジックを実行して Top-K を抽出します。



### 3. クエリ変形（Query Transformation）の高度化
単純な言い換えではなく、検索の「質」を変える3つの手法を実装します。

* **HyDE (Hypothetical Document Embeddings)**:
    ユーザーの質問に対し、Geminiに「仮の回答（嘘が含まれていても良い）」を一度生成させ、その**「回答のベクトル」で検索**をかけます。
    * *理由*: 質問（短い）よりも回答（長い）の方が、ドキュメント（長い）とベクトル空間上で近くなりやすいため（対照学習の特性）。
* **Step-back Prompting**:
    「ネジ 999999 の公差は？」という具体的すぎる質問から、「ネジの公差に関する一般的な規定は？」という抽象的な質問を生成し、上位概念の資料も同時に取得します。
* **Sub-query Decomposition**:
    「VPNが繋がらず、かつネジの在庫も知りたい」といった複合質問を、独立した2つの検索クエリに分解し、並列実行します。

### 4. Firestore Vector Search のインデックス・チューニング
3,000人規模のデータ量になると、全探索（Flat Search）はレイテンシの面で不可能です。

* **HNSW (Hierarchical Navigable Small World)**:
    Firestoreが内部で採用しているグラフベースの近似最近傍探索（ANN）インデックスを利用します。
* **M (最大接続数) と ef_construction の調整**:
    インデックス作成時のパラメータを調整し、検索速度（レイテンシ）と精度（Recall）のトレードオフを最適化します。
    * *エンジニアの視点*: 「ネジ番号」のように一点突破が必要なデータには、HNSWの層を深くするか、メタデータでの事前フィルタリング（Pre-filtering）を徹底し、探索範囲を物理的に絞り込む設計を死守します。

### 5. ネガティブ・サンプリングと閾値管理
検索結果が「ハズレ」だった場合に LLM を黙らせるための実装です。

* **Distance Thresholding**:
    Cosine Similarity が $0.75$ 未満のチャンクは、たとえ検索順位1位でもコンテキストから除外します。
* **Relevant Score Normalization**:
    複数の検索手法のスコアを 0〜1 に正規化し、検索結果の「自信度」を LLM にシステムプロンプトとして伝えます（例：「以下の情報は信頼度 60% の資料です」）。

---

## エンジニアとしての「設計の深み」

第1弾で失敗した最大の理由は、おそらく**「ユーザーの言葉を信じすぎて、そのままベクトル空間に投げ込んだこと」**にあります。
第2弾では、**「検索の前にクエリをLLMで調理し（HyDE/RRF）、検索後にスコアで冷酷に切り捨てる」**というフィルタリング・パイプラインを構築することが、精度追求の正体です。

---

### 次回予告：第4回「リランキング（再順位付け）とコンテキスト最適化」
「良さそうな候補」を30件持ってきた後、いよいよ本命の **Vertex AI Ranking API** が登場します。
* ベクトル検索では不可能な「文脈のミクロな一致」をどう判定するか。
* **Cross-Encoder** モデルによる二次評価の仕組み。
* Gemini 1.5 Pro の 200万トークンという「広大な作業机」をどう使い、情報を並べるのが最も賢い回答を生むのか。

ここまでの「数学的なスコアリング」や「インデックスの挙動」について、さらに特定のアルゴリズムや実装コードベースで掘り下げたい部分はありますか？あるいは、このまま第4回の「選別の極意」へ進みますか？