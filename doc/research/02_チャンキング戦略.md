第2回、開始します！
第1回で「綺麗なデータ」を吸い上げるパイプラインを作りました。次は、そのデータを**「どう切り分け（チャンキング）」**、**「どうラベル付け（メタデータ）」**するかです。

ここはRAGの精度、特に「情報の見落とし」や「文脈の欠落」を防ぐための最重要工程です。3000人規模の多様なデータ（長大なマニュアルから、1行の部品表まで）を扱うためのエンジニアリング手法を深掘りします。

---

## 第2回：高度なチャンキング戦略とメタデータ設計

RAGにおいて、検索エンジンが探し出す最小単位を「チャンク」と呼びます。このチャンクの作り方が悪いと、LLMに「断片的な情報」しか伝わらず、回答が不正確になります。



### 1. 「意味の切れ目」を捉えるセマンティック・チャンキング
固定長（例：500文字ずつ）で機械的に切ると、重要な文章が真っ二つに分断されるリスクがあります。

* **Recursive Character Text Splitter（再帰的文字分割）**:
    * 段落(`\n\n`) → 文(`\n`) → 句(`,`) の順で、意味の区切りを優先して分割サイズを調整します。
* **セマンティック・チャンキング（高度な手法）**:
    * 文章間の「ベクトルの類似度」を計算し、意味が大きく変わる地点を自動で検出して区切ります。Google Cloudの **Vertex AI Embedding API** を使って、隣接する文どうしの関連性をスコアリングし、境界線を引く実装が有効です。

### 2. コンテキストを維持する「チャンク・オーバーラップ」
隣り合うチャンクにあえて「重なり（Overlap）」を持たせます。

* **なぜ必要か？**: 
    * チャンクAの最後に「その理由は...」とあり、理由がチャンクBにある場合、Aだけを検索してもAIは理由を答えられません。
* **エンジニアの工夫**: 
    * 通常10%〜20%程度のオーバーラップを設定しますが、ITマニュアルのような手順書では、**「前の手順の1ステップ分」を常に含める**といったロジックを組むと、手順の取りこぼしが激減します。

### 3. 「ネジ番号問題」を解決するヘッダー・インジェクション
Excelや表形式のデータを扱う際の決定的な工夫です。

* **課題**: 
    * 表の「100行目」だけを切り出すと、それが「何の項目か（ヘッダー）」が分からなくなります。
* **実装**: 
    * 各チャンクの冒頭に、**親ドキュメントのタイトルや表の見出しを強制的に結合**します。
    * 例：「[ネジ番号: 999999 / 仕様書] 材質: SUS304...」
    * こうすることで、どの断片を拾ってもAIが「これはネジ999999の話だ」と確信を持って回答できるようになります。

### 4. 検索効率を極めるメタデータ設計
ベクトル検索（曖昧検索）に、Firestoreの強力なフィルタリング（厳密検索）を組み合わせるための「タグ付け」です。



* **必須メタデータの例**:
    * `category`: `it_helpdesk`, `parts_catalog`, `hr_policy` （検索範囲の限定用）
    * `security_level`: `general`, `admin_only` （権限による出し分け）
    * `doc_id`: 元ファイルへの紐付け
    * `updated_at`: 最新情報の優先度付け用
* **Firestoreでのハイブリッド・クエリ**:
    * 「`category == 'parts_catalog'` の中から、ベクトルが近いものを探す」という**フィルタ付き検索**を実行します。これにより、ITヘルプの質問にネジの回答が混ざるノイズを物理的に遮断できます。

---

## エンジニアへのアドバイス：第2回のチェックポイント

第1弾で精度が出なかった原因の多くは、**「情報の断片化（Fragmented Context）」**です。

1.  **チャンクが小さすぎて意味が通らなくなっていませんか？**
2.  **逆に大きすぎて、検索ノイズ（関係ない話）が混ざっていませんか？**
3.  **「ネジ番号」というキーワードが、全チャンクにメタデータとして付与されていますか？**

---

### 次回：第3回「セマンティック検索とハイブリッド検索の設計」
データが最適に切り分けられたら、次はいよいよ「探し方」の極意です。
* 「似ている（ベクトル）」と「一致している（キーワード）」をどう1つのスコアにまとめるか。
* Google Cloudでの**ハイブリッド検索の実装ロジック**。
* 検索の「漏れ」をなくすためのクエリ拡張技術。

ここまでの「切り分け方（チャンキング）」について、特に「マニュアルの図表はどう切るべきか？」や「メタデータの管理方法」などで気になる点はありますか？あるいは、今お持ちのデータで「これ、どう切ればいいの？」という具体的な例があれば教えてください。